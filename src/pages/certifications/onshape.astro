---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Pagination from '../../components/Pagination.astro';
const base = '/robotics-portfolio';

const previous = 'nc3-mechatronics';
const next = null;
---

<BaseLayout title="Onshape Certifications" base={base}>
  <Pagination base={base} section="certifications" previous={previous} next={next} />
  
  <h1>Onshape Certifications</h1>
  
  <section>
    <h2>About This Certification</h2>
    <p>
      Onshape is a cloud-based CAD (Computer-Aided Design) platform that enables collaborative 3D modeling and design. 
      The CAD Basics certification demonstrates proficiency in fundamental CAD concepts and skills essential for 
      engineering and robotics design work. I also completed the CAD for robotics training pathway. This taught me about how CAD is used in real-world engineering / robotics applications.
      However, I am unable to show the documents for that course at this time due to limitations regarding the display of .docx files on webpages.
    </p>
    <p>
      Through these 2 certifications (Basics and for Robotics), I was able to gain practice and learning in creating 3D models, assemblies, and technical drawings. 
      The coursework of both pathways covered key topics including sketching, part modeling, assembly constraints, and design documentation. 
    </p>
    <p>
      CAD for Robotics additionally covered specialized topics such as manufacturing considerations, custom component creation, 
      design communication, collaboration workflows, and drivetrain assembly—all essential skills for competitive robotics 
      and real-world engineering applications.
    </p>
  </section>

  <section style="margin-top: 2rem;">
    <h2>CAD Basics Certificate</h2>
    <figure>
      <img 
        src={`${base}/certificates/onshape.png`} 
        alt="Onshape CAD Basics Certificate" 
        style="width: 100%; max-width: 800px; height: auto; border: 1px solid #ccc; border-radius: 4px;"
      />
      <figcaption style="text-align: center; font-size: 0.9rem; margin-top: 0.5rem; color: #555;">
        Onshape CAD Basics Certification
      </figcaption>
    </figure>
  </section>

  <!-- CAD for Robotics Carousel -->
  <section style="margin-top: 3rem;">
    <h2>CAD for Robotics Coursework</h2>
    <p>Selected screenshots from the Onshape CAD for Robotics training pathway:</p>

    <div class="carousel-container">
      <div class="carousel">
        <img src={`${base}/certificates/cad-for-robotics/1.png`} alt="CAD for Robotics Screenshot 1" class="carousel-image active" />
        <img src={`${base}/certificates/cad-for-robotics/2.png`} alt="CAD for Robotics Screenshot 2" class="carousel-image" />
        <img src={`${base}/certificates/cad-for-robotics/3.png`} alt="CAD for Robotics Screenshot 3" class="carousel-image" />
        <img src={`${base}/certificates/cad-for-robotics/4.png`} alt="CAD for Robotics Screenshot 4" class="carousel-image" />
        <img src={`${base}/certificates/cad-for-robotics/5.png`} alt="CAD for Robotics Screenshot 5" class="carousel-image" />
        <img src={`${base}/certificates/cad-for-robotics/6.png`} alt="CAD for Robotics Screenshot 6" class="carousel-image" />
      </div>

      <div class="carousel-controls">
        <button class="carousel-btn prev" aria-label="Previous image">←</button>
        <span class="dot active" data-index="0">○</span>
        <span class="dot" data-index="1">○</span>
        <span class="dot" data-index="2">○</span>
        <span class="dot" data-index="3">○</span>
        <span class="dot" data-index="4">○</span>
        <span class="dot" data-index="5">○</span>
        <button class="carousel-btn next" aria-label="Next image">→</button>
      </div>
    </div>
  </section>
</BaseLayout>

<style is:global>
  .carousel-container {
    max-width: 800px;
    margin: 2rem auto;
  }

  .carousel {
    position: relative;
    width: 100%;
    aspect-ratio: 16 / 9;
    border: 1px solid #000;
    background: #fff;
  }

  .carousel-image {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    object-fit: contain;
    opacity: 0;
  }

  .carousel-image.active {
    opacity: 1;
  }

  .carousel-controls {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 1rem;
    margin-top: 1rem;
  }

  .carousel-btn {
    background: none;
    border: none;
    cursor: pointer;
    font-size: 1.5rem;
    padding: 0;
  }

  .dot {
    cursor: pointer;
    font-size: 1.5rem;
  }

  .dot.active {
    color: #000;
  }

  .dot:not(.active) {
    color: #999;
  }
</style>

<script is:inline>
  let currentIndex = 0;
  const images = document.querySelectorAll('.carousel-image');
  const dots = document.querySelectorAll('.dot');
  const totalImages = images.length;

  function showImage(index) {
    images.forEach((img, i) => {
      img.classList.toggle('active', i === index);
    });
    dots.forEach((dot, i) => {
      dot.classList.toggle('active', i === index);
    });
  }

  function nextImage() {
    currentIndex = (currentIndex + 1) % totalImages;
    showImage(currentIndex);
  }

  function prevImage() {
    currentIndex = (currentIndex - 1 + totalImages) % totalImages;
    showImage(currentIndex);
  }

  document.querySelector('.next')?.addEventListener('click', nextImage);
  document.querySelector('.prev')?.addEventListener('click', prevImage);

  dots.forEach((dot, index) => {
    dot.addEventListener('click', () => {
      currentIndex = index;
      showImage(currentIndex);
    });
  });
</script>
